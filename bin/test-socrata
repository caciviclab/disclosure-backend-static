#!/usr/bin/env python

# make sure to install these packages before running:
# pip install pandas
# pip install sodapy

import pandas as pd
from sodapy import Socrata

# Unauthenticated client only works with public data sets. Note 'None'
# in place of application token, and no username or password:
client = Socrata("data.oaklandca.gov", 'GsNm1Q31dzmwLXBybeqrcQqbM')

# Example authenticated client (needed for non-public datasets):
# client = Socrata(data.oaklandca.gov,
#                  MyAppToken,
#                  username="user@example.com",
#                  password="AFakePassword")

# First 2000 results, returned as JSON from API / converted to Python list of
# dictionaries by sodapy.
results = client.get("3xq4-ermg", limit=2000)

# Convert to pandas DataFrame
results_df = pd.DataFrame.from_records(results)

print(results_df)

filename = 'test-socrata.csv'
print(f'Socrata was called for data from 3xq4-ermg, which will be saved to {filename}')
results_df.to_csv(filename, index=False)
